node_modules/
.env
config.json
.data/
sessions.json
uploads/
*.log
.DS_Store
.npmrc
.vercel/
.env*.local

# TWA Digital Asset Links (per-build signing fingerprint — not committed)
client/.well-known/

# Built TWA APK (per-build, signed — not committed)
client/anyagent-bridge.apk
